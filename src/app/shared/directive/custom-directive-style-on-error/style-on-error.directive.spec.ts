import { StyleOnErrorDirective } from './style-on-error.directive';

describe('StyleOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
